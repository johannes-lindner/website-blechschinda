{
  "name": "blechschinda-de",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev --local"
  },
  "devDependencies": {
    "wrangler": "^3.0.0",
    "@cloudflare/kv-asset-handler": "^0.3.0"
  }
}
